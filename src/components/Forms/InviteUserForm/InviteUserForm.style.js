import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  form: {
    backgroundColor: "#F8F8F8",
    paddingTop: "0.4rem",
    paddingBottom: "1rem",
  },
  invite: {
    marginTop: "1rem",
    textAlign: "right",
  },
});
