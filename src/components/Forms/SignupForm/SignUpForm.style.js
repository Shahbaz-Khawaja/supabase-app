import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  form: {
    backgroundColor: "#F8F8F8",
    marginBottom: "2rem",
  },
  createAccountBtn: {
    marginTop: "1rem",
    textAlign: "right",
  },
  terms: {
    marginTop: "1.2rem",
  },
  action: {
    display: "flex",
    alignItems: "baseline",
  },
});

export const style = {
  link: {
    fontWeight: "bold",
    fontSize: "13.5px",
  },
};
